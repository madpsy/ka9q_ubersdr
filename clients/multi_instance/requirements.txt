# Multi-Instance Spectrum Client Requirements
# Minimal dependencies for spectrum display only (no audio)

# Core dependencies
requests>=2.31
websockets>=15.0
websocket-client>=1.6.0
aiohttp>=3.9.0
numpy>=1.24.0
matplotlib>=3.7.0
zeroconf>=0.132.0

# Optional: MIDI controller support
python-rtmidi>=1.5.0

# Optional: For public instances display and local discovery
# (same as main client, but audio libraries not needed)